# 交付验证记录

日期：2026-09-20。环境：Windows、Node 24、Blender 4.5.3 LTS。

## 数据与模型

- 实际 OSM 下载时间：2026-09-20T07:38:09.767Z；服务器数据时间：2026-09-20T07:36:35Z。83 个原始元素，原始响应与 SHA256 存于 public/data。
- 场景包含 64 个建筑对象、340 棵程序化树木、84 个模拟车位。建筑对象数量不是官方住宅栋数；西北侧 15 栋低层建筑来自截图近似补绘。
- 两栋重点建筑的位置依据用户补充标注。高度、立面、树种造型与内部设施位置均为近似重建。
- Blender 实际生成 campus.blend、三个 GLB 和 Cycles 64 采样的 1600×1200 日景图，已人工查看渲染结果。
- 重新打开保存的 Blender 文件验证通过：64 个建筑、220 个对象、68 个带动画对象、24 fps、21601 帧、相机远裁剪 5000 m、CYCLES 引擎。
- Web 模拟包含 72 名行人和 66 辆车；Blender 动画使用同一时间轨迹数据，行人抽样为 12 名，包含 8 辆动态车辆及 58 辆静态停放车辆。

## 程序与浏览器

- `npm test`：10 项通过，覆盖拾取父节点、确定性回放、暂停与倍速、停车容量和唯一车位、车辆连续性、路径边界、告警历史、历史动作不可覆盖、行人与车辆路径避开建筑、内部道路中心线避开建筑。
- `npm run build`：TypeScript 类型检查与 Vite 生产构建通过。
- 在实际浏览器加载最终 GLB，WebGL 正常连接，最终控制台错误列表为空。日间与夜间页面截图保存于 screenshots。
- 已操作验证：楼宇搜索定位、模型点击拾取、总览及视角切换、日夜切换、图层、停车状态、生成告警、确认与解除、事件回放、暂停与播放、数据来源弹窗。
- 390×844 移动视口无横向溢出，资产面板展开可用；1280×720 右侧面板允许滚动，告警区域设置最小高度以保证按钮可操作。
- 现场观察帧率约 30–36 FPS，受设备与窗口尺寸影响，不构成性能保证。

## 实际限制

公开网页与图片搜索未找到可核实属于这两栋重点楼的清晰立面照片，因此没有声称按真实照片完成精确外观复原。此项目为可运行的三维模拟与近似空间重建，非测绘成果。树木为程序化阔叶树近似，非植物级写实扫描模型；人物、车辆、运行指标和告警没有连接真实设备。

告警操作记录保存在当前页面内存中，刷新会恢复初始演示状态。数据再生成和模型再渲染步骤见 README。

## 后续实景照片修订（2026-09-20）

以上未获得立面照片的记录为初版研究状态，现由用户补充的两张照片更新：照片 1 为国际部大楼，照片 2 为装备专业化大楼入口。新增 photo_facades.py 重建可见建筑细节；未见侧后立面与实际尺寸仍为推定，照片没有作为含车辆和遮挡物的平面贴图。

## 楼层确认修订验证

用户确认国际部 20 层、装备楼 6 层。已修改共享数据、可再生成脚本、模型几何、GLB 元数据及网页标签。国际部高度暂按 3.6 m 平均层高估为 72 m，装备楼暂沿用 25 m 高度估计。自动化测试 10/10 通过，生产构建通过；浏览器中两项“已确认”层数标签均验证存在，WebGL 控制台无错误。打包校验增加了完整园区与独立 GLB 中的楼层元数据断言。
